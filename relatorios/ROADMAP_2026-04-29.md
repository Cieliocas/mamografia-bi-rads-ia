# AIdentify — Roadmap de Próximos Passos
**Data:** 2026-04-29
**Branch ativa:** `claude/interesting-khayyam-962c0b`
**PR aberto:** https://github.com/Cieliocas/mamografia-bi-rads-ia/pull/4

---

## Estado Atual — Todos os 6 Planos Concluídos ✅

| Plano | Descrição | Status |
|-------|-----------|--------|
| Plan 01 | Go Clean Architecture (domain, ports, use cases, adapters) | ✅ |
| Plan 02 | Monorepo (desktop Wails, frontend Angular, core Go, AI engine Python) | ✅ |
| Plan 03 | Feature-Sliced Angular (ViewerStateService, StudyService, componentes) | ✅ |
| Plan 04 | Integração Go ↔ Angular (ApiService, signals, OpenFileDialog, history) | ✅ |
| Plan 05 | AI Sidecar Python (FastAPI, mock findings, pipeline Go ↔ Python) | ✅ |
| Plan 06 | Export & Relatório (JSON/CSV download, HTML report com auto-print PDF) | ✅ |

---

## Próximos Passos Prioritários

### 🔴 Imediato (Bloqueia progresso)

#### 1. Merge PR #4 → main
- **O que fazer:** Acessar https://github.com/Cieliocas/mamografia-bi-rads-ia/pull/4 e fazer merge
- **Por quê:** O PR tem 29 commits com todos os 6 planos completos; ainda está em draft/open

#### 2. Fix Wails Dev Mode
- **Problema:** `wails.json` tem `"frontend:dev:watcher": "npm --prefix ../frontend start"` mas Wails procura `apps/desktop/frontend/` (diretório local que não existe)
- **Solução:** Criar symlink `apps/desktop/frontend → ../frontend` OU ajustar `wails.json` para apontar corretamente
- **Workaround atual:** Build manual do Angular + `python3 -m http.server 4300`

---

### 🟡 Alta Prioridade (Funcionalidade core)

#### 3. DICOM Real (go-dicom)
- **O que fazer:**
  - Adicionar `go-dicom` ao `apps/core/go.mod`
  - Implementar `DICOMReader.ReadPixels()` real: extração de pixel data 16-bit, WW/WC do header
  - Converter array de pixels para PNG/canvas no frontend
- **Ficheiros chave:** `apps/core/internal/infrastructure/dicomreader/` (stub atual)
- **Impacto:** Sem isso, o app não lê arquivos `.dcm` reais

#### 4. Modelo ONNX Real no Sidecar Python
- **O que fazer:**
  - Integrar `onnxruntime` no `apps/ai-engine/requirements.txt`
  - Substituir mock em `apps/ai-engine/app/main.py` por inferência real
  - Definir formato do modelo: YOLOv8/ONNX para detecção de achados mamários
- **Impacto:** Sem isso, o "Rodar IA" retorna sempre os mesmos 2 achados sintéticos

#### 5. CI/CD (GitHub Actions)
- **O que fazer:** Criar `.github/workflows/ci.yml` com:
  - `go test ./...` no `apps/core/`
  - `ng build` no `apps/frontend/`
  - `pytest` no `apps/ai-engine/`
  - `wails build` para gerar `.app` macOS
- **Impacto:** Garante que PRs futuros não quebrem o pipeline

---

### 🟢 Média Prioridade (Qualidade e UX)

#### 6. Testes Automatizados
- **Go:** `httptest` para todos os handlers HTTP
- **Angular:** Jest para `ViewerStateService`, `StudyService`, componentes
- **Python:** `pytest` + `httpx` para `POST /predict`

#### 7. UX — Loading States e Feedback
- **O que fazer:**
  - Spinner/skeleton durante carregamento de imagem e inferência
  - Toast notifications (sucesso/erro) para save, export, inference
  - Progress bar real para carregamento de DICOM grandes
  - Mensagens de erro amigáveis quando Go Core está offline

#### 8. Build de Distribuição
- **O que fazer:**
  - Resolver `//go:embed all:dist` em `apps/desktop/main.go` — Angular build deve ir para `apps/desktop/dist/`
  - Testar `wails build` → `.app` macOS assinado
  - Considerar `wails build -platform windows/amd64` para Windows

---

### 🔵 Futuro (Roadmap longo prazo)

| Item | Descrição |
|------|-----------|
| DICOM SR | Structured Reports — exportar achados como DICOM SR padrão radiológico |
| Multi-monitor Split View | Comparação bilateral (CC + MLO) em monitores separados |
| Integração RIS/PACS | Conectar ao Picture Archiving and Communication System |
| Audit Log | Rastrear todas as ações de médicos radiologistas |
| Multi-tenant | Suporte a múltiplos hospitais/clínicas |

---

## Issues Conhecidos

| Issue | Severidade | Workaround |
|-------|------------|------------|
| Wails dev mode — npm não encontrado | Alta | Build manual Angular + python3 http.server |
| Sidecar Python — sem modelo real | Alta | Mock findings sintéticos |
| DICOMReader — stub sem parse real | Alta | Apenas imagens JPEG/PNG |
| `go-sqlite3` requer CGO/gcc | Média | Documentar na instalação |

---

## Comandos Úteis

```bash
# Frontend
cd apps/frontend && npm run build
cd apps/frontend && npx ng serve --port 4200  # dev

# Go Core
cd apps/core && go build -o bin/core ./cmd/server && ./bin/core

# Python sidecar
cd apps/ai-engine && .venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8090

# Testar pipeline
curl http://127.0.0.1:8088/healthz
curl http://127.0.0.1:8088/readyz
curl -X POST http://127.0.0.1:8088/api/tasks/predict \
  -H "Content-Type: application/json" \
  -d '{"image_path":"/tmp/test.jpg"}'

# Git
git checkout claude/interesting-khayyam-962c0b
git push origin claude/interesting-khayyam-962c0b
```

---

## Variáveis de Ambiente

| Variável | Default | Descrição |
|----------|---------|-----------|
| `AI_ENGINE_URL` | `http://127.0.0.1:8090` | URL do sidecar Python |
| `AI_ENGINE_PYTHON` | auto-resolved | Caminho do Python para o Guardian |
| `AI_SHARED_TOKEN` | *(gerado automaticamente)* | Token de autenticação sidecar — gerado em `~/.mammo-desktop/.token` na primeira execução |
| `DB_PATH` | `./mammo.db` | Caminho do SQLite |

---

*Gerado automaticamente ao final da sessão de desenvolvimento — Plans 01–06 completos.*
